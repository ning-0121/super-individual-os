{
  "name": "sio-local-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Super Individual OS — local agent runner (V0.1, read-only)",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "once": "tsx src/index.ts --once"
  },
  "engines": { "node": ">=18" },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
